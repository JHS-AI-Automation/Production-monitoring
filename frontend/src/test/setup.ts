// Vitest setup: extra DOM-matchers (toBeInTheDocument, ...) via de vitest-entry
// van jest-dom, zodat ze op vitest's expect worden geregistreerd (geen globals nodig).
import "@testing-library/jest-dom/vitest";
